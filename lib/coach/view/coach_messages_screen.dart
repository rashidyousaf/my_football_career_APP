import '../../consts/consts.dart';

class CoachMessagesScreen extends StatelessWidget {
  const CoachMessagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: whiteColor,
    );
  }
}
