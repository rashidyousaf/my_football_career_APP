import '../../consts/consts.dart';

class PlayerMessagesScreen extends StatelessWidget {
  const PlayerMessagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: greenColor,
    );
  }
}
